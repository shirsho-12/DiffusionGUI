import 'package:flutter/material.dart';

class Break extends StatelessWidget {
  final bool stageOne;

  const Break({required this.stageOne, Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
